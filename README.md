# duckdb-builds

Builds statically-linked DuckDB for Linux AMD64 and ARM64 with a set of extensions pre-compiled, in two variants of extension loading: locked and unlocked. The release artifacts contain `libduckdb_bundle.a`, DuckDB's `LICENSE`, and headers in `include`.
